using Core;
class Example
{
    static void Main()
    {
        //no async
        // string neofetch = CoreCommands.ExecShellCommand("neofetch");

        // with async output
        // IEnumerable<string> args = new string[]{ "8.8.8.8" };
        // string ping = CoreCommands.ExecShellCommandAsync("ping", args);
    }
}